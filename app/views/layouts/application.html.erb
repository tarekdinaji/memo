<!DOCTYPE html>
<html>
  <head>
    <!-- 
    This tag defines the head section of the HTML document. 
    It contains meta-information about the document, such as its title, 
    links to scripts and stylesheets, and other metadata. 
    -->
    <title>Memoio</title>
      <!-- 
      this part appears in the browser tab 
      -->
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/navbar" %>
      <%# -- need to show notice -- %>
        <% if notice %>
        <%= notice %>
        <% end %>
        <% if alert %>
          <%= alert %>
        <% end %>
      <%# -- end -- %>
    <%= yield %>
  </body>
</html>
